package com.example.tugaskelompok

import androidx.appcompat.view.menu.MenuView.ItemView

class ClubAdapter (private val clubList:ArrayList<DetailClub>) {
    class DetailViewHolder(itemView: ItemView) : ReycycleView.ViewHolder()
}
